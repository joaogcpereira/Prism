{
  "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
  "Prism": {
    "//ConnectionString": "Warehouse (read-only). If null, the UI serves its built-in sample data.",
    "ConnectionString": null
  }
}
